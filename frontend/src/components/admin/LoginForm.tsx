'use client';import {useState,type FormEvent} from 'react';import {useRouter} from 'next/navigation';import {apiFetch} from '@/lib/api';
export function LoginForm(){const router=useRouter();const [state,setState]=useState({loading:false,error:''});async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);setState({loading:true,error:''});try{await apiFetch('/admin/auth/login',{method:'POST',body:JSON.stringify({email:f.get('email'),password:f.get('password')})});router.replace('/admin');router.refresh();}catch(error){setState({loading:false,error:error instanceof Error?error.message:'Login failed.'});}}return <form className="admin-login-card" onSubmit={submit}><div className="admin-login-brand">Lead <b>Hippo</b></div><h1>Admin sign in</h1><p>Secure access to lead, purchase and contact management.</p><label>Email<input name="email" type="email" autoComplete="username" required/></label><label>Password<input name="password" type="password" autoComplete="current-password" required minLength={8}/></label><button className="button full" disabled={state.loading}>{state.loading?'Signing in…':'Sign In'}</button>{state.error&&<p className="form-message error">{state.error}</p>}<small>The default development credentials are documented in database/seed.sql. Change them before deployment.</small></form>}
